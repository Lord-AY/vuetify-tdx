export default {
  namespaced: true,
  state: {},
  getters: {},
  actions: {
    // this is a test to try to get user object from auth
    // getStates({ rootState }) {
    //   // use this to rootState.auth.user to get user object
    //   // inside the auth state, u can fetch id using rootState.auth.user.id etc.
    // 	console.log(rootState.auth.user)
    // }
  },
  mutations: {}
};
